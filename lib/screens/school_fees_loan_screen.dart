import 'package:flutter/material.dart';

class SchoolFeesLoanScreen extends StatelessWidget {
  const SchoolFeesLoanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('School Fees Loan'),
      ),
      body: const Center(
        child: Text('Details about School Fees Loan'),
      ),
    );
  }
}
